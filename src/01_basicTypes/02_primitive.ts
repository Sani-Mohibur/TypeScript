//JavaScript is a dynamically typed language.
//TypeScript is a statically typed language.
//JS - string, number, boolean, undefined, null
//Ts - never, unknown, void

let userName: string = "Farabi Sunny";

let userId = 1234;

let isAdmin = false; //implicit
isAdmin = true;

let isModerator: boolean = true; //explicit
isModerator = false;

//Both datatype is any that means in later can be assignable any datatype (string, number, etc)
let x: any;
let y;

let z: undefined = undefined;
